 def set_a(self, value):
        self.a = value
    
    def get_a(self):
        return self.a
# Вывод документации класса
print(A.__doc__)
# Создание первого экземпляра класса
a1 = A()
# Установка значения атрибута a при помощи метода set_a()
a1.set_a(5)
# Вывод значения атрибута a при помощи метода get_a()
print(a1.get_a())
# Создание второго экземпляра класса
a2 = A()
# Установка значения атрибута a при помощи прямого доступа к атрибуту по точке
a2.a = 10
# Вывод значения атрибута a
print(a2.a)

Вывод:
Класс A
5
10
